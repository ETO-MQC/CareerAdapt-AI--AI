# 职适AI项目协作规范

## 权威文档

### V1历史档案

1. `职适AI_产品需求文档_PRD_v1.0.md`
2. `Plan.md`
3. `history.md`

`Plan.md` 与 `history.md` 是第一代 MVP 历史档案。除第一代严重缺陷修复和交付补记外，不再作为第二代开发入口。

### V2默认读取顺序

第二代开发前默认依次阅读：

1. `plan2.md`
2. `history2.md` 最近相关记录
3. `docs/MVP_V1_HANDOFF.md`
4. `docs/V2_START_HERE.md`
5. 当前 Goal 对应的 `docs/v2/*` 设计文档
6. 当前需要修改的源码

只有涉及 V1 迁移、V1 历史 Bug 或旧设计争议时，才默认加载完整 `Plan.md` 与 `history.md`。

## 当前开发原则

- 每次只完成当前指定阶段，不得提前开发后续阶段。
- 任何不能增强MVP纵向闭环的功能进入后置池。
- 不得擅自修改产品定位、核心数据职责或技术栈。
- 不得把API密钥写入前端、源码或Git仓库。
- 不得用自由文本直接覆盖核心业务数据，AI结果必须通过Schema校验。
- 未经用户确认的新事实不得进入简历预览和PDF导出。
- 不得删除已有功能来规避测试失败。
- 不得进行破坏性Git操作。
- 如果遇到模棱两可的，应该停下来询问以防止开发偏移

## 开发流程

每个开发任务必须：

1. 先检查现有代码和文档，不重复实现已有能力。
2. 先写或更新类型和Schema，再实现业务功能。
3. 完成后运行类型检查、Lint、测试和构建。
4. 修复本次修改引入的问题。
5. V1任务更新 `Plan.md` / `history.md`；V2任务更新 `plan2.md` / `history2.md`。
6. 记录修改文件、验证结果、遗留问题和下一步。
7. 给出简明的交付总结。

## 完成标准

只有同时满足以下条件才可宣告完成：

- 对应功能真实可运行，不是占位实现。
- 相关Schema、测试和错误状态已覆盖。
- 类型检查、Lint、测试和构建通过。
- `Plan.md`与`history.md`已经更新。
- 未越过当前阶段范围。
