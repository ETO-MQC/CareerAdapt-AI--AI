# Claude 项目开发约束

## 核心原则

不要乱删、乱改代码。修改一行代码前，先想清楚：它被谁调用？删掉后会不会影响导入、档案、JD、匹配、建议、分支、模板、PDF 导出等核心功能？

## 开发顺序

必须按 `Plan.md` 的顺序开发。优先完成“职业母档案 -> 岗位适配 -> 可解释生成 -> PDF导出”的闭环；与闭环无关的功能先后置。

当前执行以 `Plan.md` 的“五阶段比赛版路线”为准：先做底座与最小技术验证，再做母档案与 JD，再做匹配建议与 Fact Guard，然后做分支模板导出，最后补 PDF 导入和比赛材料。

## 修改纪律

- 修改前先读相关文件和调用关系。
- 不做无关重构，不大面积改风格。
- 不删除不理解的代码、配置、数据或文档。
- 需要删除文件时，只能一次删除一个明确路径的文件；禁止批量删除目录或文件。
- AI 建议、Prompt、Schema、业务规则和 UI 代码要分层放置，不要互相混在一起。
- 母档案事实层、岗位分支表达层、模板渲染层必须保持边界清楚。
- 所有文件引用统一使用当前大小写：`Plan.md`、`history.md`、`Claude.md`。

## MVP硬约束

- 每次开发都要保持已有 Demo 路径可运行。
- 先支持粘贴文本闭环，PDF 导入不能阻塞主链路。
- 所有进入最终简历的事实必须记录来源、确认状态和风险状态。
- AI 输出必须经过 Schema 校验，不能把自由文本直接写入核心数据。
- AI 调用必须走统一 AI Service，不允许各模块直接散落调用模型。
- API 密钥只能放在服务端或本地环境变量中，不能写入前端代码或仓库。
- MVP 不建设向量数据库，不做复杂分支自动合并，不新增两套模板之外的模板。

## 每次完成后

1. 更新 `Plan.md`：标记已完成、进行中、阻塞或后置任务。
2. 更新 `history.md`：写清楚本次修改、验证结果、遗留问题。
3. 在 `Plan.md` 写明下次开发路线。
4. 至少记录一个验证结果；阶段完成时尽量产出一张比赛素材截图。

## 事实与合规

- 不生成用户无法证明的经历、成果、数字或奖项。
- 未经用户确认的新事实不能进入正式简历或导出结果。
- 不做验证码绕过、Cookie 窃取、反风控绕过或无人值守批量投递。
- 简历和证据属于敏感信息，默认最小化收集、最小化发送给第三方模型。
