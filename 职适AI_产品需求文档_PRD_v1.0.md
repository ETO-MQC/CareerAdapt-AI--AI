# 职适AI——大学生岗位自适应求职智能体

中文名：职适AI
英文名：CareerAdapt AI
英文口号：One career profile, tailored for every role.

**产品需求文档（PRD） v1.0**

| 字段 | 内容 |

| --- | --- |

| 文档版本 | v1.0 |

| 项目负责人 | 明启辰（个人参赛） |

| 参赛方向 | OPC 2026｜智联招聘 Bounty｜AI+求职 |

| 当前阶段 | 原型设计与 MVP 开发准备 |

| 文档日期 | 2026年7月1日 |

| 一句话介绍 | 导入一次经历，生成每个岗位专属的简历与求职方案。 |

> **产品核心：职业母档案 × 岗位理解 × 真实经历匹配 × 可解释生成**


---


# 0. 文档说明

## 0.1 文档目的

本PRD用于统一职适AI的产品定位、用户问题、功能范围、AI工作流、数据模型、验收标准和比赛交付路径，并作为后续原型设计、技术选型、开发排期、测试验收及演示视频制作的共同依据。

文档面向单人开发场景设计：优先保证两周内可构建、可演示、可解释的最小闭环，避免在早期陷入高精度OCR、原版式复刻、自动批量投递和复杂企业后台等高成本模块。

## 0.2 版本记录

| 版本 | 日期 | 负责人 | 主要变更 |

| --- | --- | --- | --- |

| v1.0 | 2026-07-01 | 明启辰 | 首次完整PRD：明确MVP闭环、Agent设计、数据模型与比赛验收标准 |

## 0.3 术语定义

| 术语 | 定义 |

| --- | --- |

| 职业母档案 Career Master Profile | 用户全部真实经历、技能、成果、证据和表达偏好的统一结构化数据源，不等同于任何一份最终简历。 |

| 岗位JD | 岗位描述，包括职责、要求、加分项、公司与行业信息。 |

| 岗位分支 Resume Branch | 基于职业母档案，为某一具体岗位生成的专属简历版本。 |

| 证据 Evidence | 能够证明经历真实性的证书、作品、报告、链接、数据或文件。 |

| AI建议卡片 | 包含原文、建议文案、修改原因、对应岗位要求、事实风险和操作按钮的可解释修改单元。 |

| Agent/Skill | 负责特定任务的AI执行单元；Skill强调可复用能力，Agent强调在上下文中规划和调用能力。 |

# 1. 执行摘要

> **职适AI不是“帮用户写一份简历”，而是把用户的全部经历沉淀为职业资产，并针对每个岗位生成真实、可解释、可维护的求职版本。**

大学生求职的主要困难并非缺少模板，而是缺少把零散经历转化为岗位能力证据的能力。现有简历工具多停留在在线编辑、模板切换、通用润色或模糊ATS评分，无法系统解决“经历如何沉淀、岗位如何理解、内容为何修改、多个版本如何维护”的连续问题。

职适AI建立“职业母档案—岗位拆解—证据匹配—岗位分支—人工确认—求职材料—投递管理”的完整链路。AI承担非结构化简历解析、经历追问、岗位需求拆解、语义匹配、内容重组、事实风险检测和表达校准等核心工作；用户保留对所有事实和最终投递的确认权。

## 1.1 MVP核心闭环

```text

导入简历/粘贴文本
→ 生成结构化职业母档案
→ 用户编辑和确认经历
→ 粘贴目标岗位JD
→ AI拆解岗位要求
→ AI匹配真实经历并识别差距
→ 生成可解释修改建议
→ 用户逐项接受/拒绝
→ 形成岗位专属简历分支
→ 切换模板并导出PDF

```

## 1.2 首版差异化

- 以职业母档案为数据中心，而不是以某个简历模板或某份文件为中心。

- 同一份真实经历可生成多个岗位分支，并继承母档案更新。

- AI不只做关键词润色，而是建立“岗位要求—用户经历—证明证据”的对应关系。

- 所有AI修改均可解释、可确认、可撤销，新增事实被显式标记。

- 内容层与模板层解耦，同一数据可渲染为不同风格与语言。

- 投递采用人机协同，不绕过招聘平台规则，不进行无人值守批量投递。

# 2. 项目背景与机会判断

## 2.1 赛题背景

项目面向智联招聘“AI+求职”赛题。赛题强调大学生求职全流程、职业规划、简历、投递和面试等真实痛点，要求方案同时具备创意性、AI核心能力与落地性。职适AI选择从“简历与岗位适配”这一高频刚需切入，并向求职材料联动和投递管理延伸。

## 2.2 用户现状

- 经历零散：课程、比赛、项目、实习、校园活动分布在不同文件与记忆中。

- 表达困难：用户知道自己“做过”，但不会说明任务、行动、工具、结果与贡献。

- 岗位理解不足：看到JD后难以识别硬性条件、优先级与隐含能力要求。

- 通用简历泛化：一份简历投所有岗位，岗位相关性低。

- 重复劳动严重：每次投递都复制、删改、改文件名，版本快速失控。

- AI信任不足：生成内容可能空洞、夸张或虚构，且缺少修改依据。

- 投递过程割裂：简历、求职信、自我介绍、面试准备和投递记录彼此独立。

## 2.3 现有方案不足

| 方案类型 | 优势 | 主要缺口 |

| --- | --- | --- |

| 传统Word/模板 | 自由度高 | 重复排版、版本混乱、无法理解岗位 |

| 在线简历编辑器 | 模板与实时预览成熟 | 通常不能高质量导入已有简历；内容被模板牵制 |

| JSON简历工具 | 结构化、可切换主题 | 普通用户不会生成JSON；AI与岗位适配弱 |

| AI润色工具 | 改写速度快 | 缺乏职业母档案、事实校验和多岗位版本管理 |

| ATS评分工具 | 提供分数和关键词建议 | 评分依据模糊，容易诱导关键词堆砌 |

| 批量投递工具 | 减少操作次数 | 易违反平台规则，造成无效投递与账号风险 |

# 3. 产品定位、愿景与设计原则

## 3.1 产品定位

面向大学生和应届毕业生，以职业母档案为核心，理解目标岗位并从用户真实经历中选择、重组和表达岗位证据的AI求职智能体。

## 3.2 产品愿景

让每一名大学生都拥有一个长期理解自己经历、尊重事实、能根据岗位持续进化的AI求职副驾。

## 3.3 价值主张

| 对象 | 价值主张 |

| --- | --- |

| 求职者 | 一次整理全部经历；每个岗位获得专属且真实的简历与材料；理解自己为何匹配或缺失。 |

| 高校就业服务 | 降低一对一简历指导成本，帮助学生形成结构化职业档案和求职能力。 |

| 招聘平台/企业 | 获得结构更清晰、岗位相关性更高、事实风险更低的候选人材料。 |

## 3.4 产品原则

1. 真实优先：不得以“提升匹配度”为由生成用户无法证明的经历、成果或数字。

2. 结构化优先：先建立稳定的数据模型，再进行AI生成和模板渲染。

3. 可解释优先：关键判断必须展示依据，避免黑箱评分。

4. 用户确认优先：AI建议不能直接覆盖原始内容或自动提交投递。

5. 闭环优先：先跑通完整链路，再扩展更多模板、模型和外围功能。

6. 内容与视觉解耦：任何模板只负责渲染，不成为数据来源。

7. 合规优先：不绕过验证码、风控、访问限制和招聘平台服务条款。

8. 隐私最小化：仅收集实现功能所需信息，允许用户随时导出与删除。

# 4. 目标用户与核心场景

## 4.1 核心用户画像

| 画像 | 典型特征 | 关键需求 |

| --- | --- | --- |

| A. 首次求职型 | 大一至大三，经历不算少但不会整理；第一次投实习 | 希望AI通过追问帮助发现可写内容，而非凭空生成。 |

| B. 多方向投递型 | 同时考虑数据、运营、外贸、产品等多个方向 | 希望一份母档案生成多个岗位版本，减少重复修改。 |

| C. 已有简历优化型 | 已有PDF或Word简历，但内容与目标岗位不匹配 | 希望直接导入、结构化、逐条获得修改建议并切换模板。 |

| D. 求职复盘型 | 已投递若干岗位但回复少 | 希望识别岗位差距、材料问题和投递结果，迭代下一轮版本。 |

## 4.2 核心Jobs To Be Done

- 当我不知道经历能否写进简历时，帮助我通过追问提取可验证的任务、行动和结果。

- 当我已有一份PDF简历时，帮助我快速转成可编辑、可复用的职业档案。

- 当我看到一个岗位JD时，帮助我理解企业真正重视什么，以及我有哪些证据可以对应。

- 当我投递不同岗位时，帮助我复用同一份经历并生成差异化版本，而不是重新复制粘贴。

- 当AI建议修改时，告诉我为什么改、用了什么依据、是否引入了新事实。

- 当我完成简历时，帮助我同步生成自我介绍、沟通文案和面试准备，确保信息一致。

## 4.3 首版场景边界

首版聚焦中文大学生实习与校招简历，优先支持常见的一页式中文简历和文本型PDF。英文简历、扫描件OCR、作品集、企业端筛选、多语言及招聘网站深度集成不作为MVP阻塞项。

# 5. 产品目标、非目标与衡量指标

## 5.1 MVP目标

1. 用户可在10分钟内完成已有简历导入并得到可编辑的结构化档案。

2. 用户可粘贴任意常见岗位JD，获得结构化岗位要求和重要度。

3. 系统可为每条岗位要求关联至少一条相关经历或明确标记“当前无证据”。

4. 用户可逐项查看、接受、拒绝或修改AI建议，且原始内容不丢失。

5. 同一职业母档案可生成至少两个岗位分支并分别导出PDF。

6. 系统至少提供两套差异明显、信息完整的一页式模板。

## 5.2 非目标

- 不承诺自动获得Offer或显著提升录用率。

- 不进行无人值守批量投递、绕过验证码或模拟真人规避检测。

- 不追求首版还原任意复杂PDF的像素级版式。

- 不替代职业规划师做长期人生决策或心理测评。

- 不在MVP阶段构建完整招聘网站、企业ATS或高校管理后台。

- 不以“规避AI检测”为产品卖点。

## 5.3 北极星指标与关键指标

| 指标类别 | 定义/目标 |

| --- | --- |

| 北极星指标 | 完成“JD导入→接受建议→导出岗位分支”的有效岗位适配次数/活跃用户 |

| 效率 | 从导入简历到导出首份岗位分支的中位时长≤15分钟 |

| 解析质量 | 核心字段正确率≥85%；关键经历漏识别率≤10%（小样本人工标注） |

| 建议质量 | AI建议被接受或部分接受比例≥50% |

| 可信度 | 未经确认的新事实自动写入正式简历的次数=0 |

| 可用性 | MVP关键任务完成率≥80%；崩溃或不可恢复错误率<2% |

| 比赛展示 | 两个不同岗位生成的简历在经历选择、顺序或表达上存在可解释差异 |

> 以上指标用于原型验证和比赛展示，不应在尚无数据时对外宣称已经达成。

# 6. 产品范围与版本规划

## 6.1 MVP（比赛可演示版）

| 优先级 | 范围 |

| --- | --- |

| P0 必须 | 本地/账号内创建职业档案；粘贴文本；文本型PDF导入；结构化编辑；JD解析；经历匹配；AI建议卡片；岗位分支；两套模板；PDF导出；撤销/历史；基础隐私提示 |

| P1 建议 | AI对话式经历挖掘；证据链接；自我介绍；打招呼文案；面试问题预测；岗位收藏；本地持久化 |

| P2 后续 | 扫描件OCR；Word导出；英文简历；更多模板；投递看板；浏览器扩展；高校端；完整面试模拟 |

## 6.2 版本路线图

| 版本 | 核心内容 | 建议时间 |

| --- | --- | --- |

| V0.1 原型闭环 | 导入/粘贴→母档案→JD→建议→分支→模板→PDF | 比赛报名后2—4天 |

| V0.2 可信增强 | 证据库、事实差异检测、自然表达模式、历史对比 | 第1周 |

| V0.3 求职联动 | 自我介绍、沟通文案、面试问题、岗位看板 | 第2周 |

| V1.0 正式提交版 | 稳定性、隐私说明、演示数据、测试报告、在线体验链接 | 7月31日前 |

| V1.5 商业验证版 | 订阅、额度、高校试点、更多模板和模型路由 | 赛后 |

# 7. 端到端用户流程

## 7.1 新用户主流程

```text

进入首页
→ 选择“导入已有简历 / 与AI对话创建 / 从空白开始”
→ 系统解析并生成职业母档案草稿
→ 用户校对字段和事实
→ 进入岗位工作区并粘贴JD
→ AI输出岗位画像、要求清单和优先级
→ 系统将岗位要求与经历建立证据映射
→ 用户查看匹配、缺失与可优化项
→ 生成AI建议卡片
→ 用户逐项接受/拒绝/编辑
→ 保存为岗位分支
→ 选择模板并检查分页
→ 导出PDF
→ 可继续生成自我介绍和面试问题

```

## 7.2 已有用户多岗位流程

```text

打开职业母档案
→ 新建岗位分支
→ 粘贴第二个JD
→ 复用母档案并重新计算匹配
→ 对比两个岗位分支的经历选择和表达
→ 独立修改岗位专属内容
→ 母档案更新时选择“同步到所有分支 / 仅未来分支 / 不同步”

```

## 7.3 AI建议确认流程

```text

AI生成建议
→ 标注修改类型：重写/补充/删减/排序/风险
→ 展示岗位依据与用户经历依据
→ 检测新增事实
→ 若存在新增事实：强制确认
→ 用户接受/部分接受/拒绝/手动编辑
→ 记录版本与操作日志
→ 更新预览，不覆盖母档案原始记录

```

# 8. 信息架构与页面清单

## 8.1 信息架构

```text

职适AI
├── 首页 / 项目空间
│   ├── 新建职业档案
│   ├── 最近岗位分支
│   └── 导入入口
├── 职业母档案
│   ├── 基本信息
│   ├── 教育 / 实习 / 项目 / 竞赛 / 校园
│   ├── 技能 / 证书 / 作品
│   └── 证据库
├── 岗位工作区
│   ├── JD原文
│   ├── 岗位解析
│   ├── 经历匹配
│   ├── 差距与风险
│   └── 岗位分支
├── 简历工作台
│   ├── 模块与版本
│   ├── 实时预览
│   ├── AI建议
│   └── 模板与导出
├── 求职材料
│   ├── 自我介绍
│   ├── 沟通文案
│   └── 面试问题
└── 设置
    ├── 模型配置
    ├── 数据与隐私
    └── 导入导出

```

## 8.2 页面列表与职责

| 页面 | 核心职责 |

| --- | --- |

| P01 首页/项目空间 | 新建、导入、最近档案与岗位分支、演示入口 |

| P02 导入向导 | 上传/粘贴、解析进度、字段确认、错误处理 |

| P03 职业母档案编辑器 | 结构化编辑全部经历；添加证据；AI追问 |

| P04 岗位JD工作区 | JD输入、结构化要求、重要度、硬性条件 |

| P05 匹配诊断页 | 要求—经历映射、已满足/可证明/缺失/风险 |

| P06 简历优化工作台 | 岗位分支、实时预览、AI建议卡片、撤销 |

| P07 模板与导出 | 模板切换、分页检查、PDF导出 |

| P08 求职材料页 | 自我介绍、打招呼文案、面试问题 |

| P09 投递看板（P1/P2） | 岗位状态、日期、备注、材料版本 |

| P10 设置与隐私 | 模型、数据存储、删除、导出、第三方说明 |

# 9. 详细功能需求

## 9.1 首页与项目空间

### 功能目标

让用户在30秒内理解产品价值并进入三种创建路径，同时快速访问最近编辑的职业档案和岗位分支。

### 功能需求

- 顶部展示项目名称、核心价值和“新建职业档案”主按钮。

- 三种入口卡片：导入已有简历、与AI对话创建、从空白开始。

- 最近项目列表展示档案名称、最后编辑时间、岗位分支数量和最近导出状态。

- 比赛演示模式可提供“加载示例数据”，无需注册即可快速体验主链路。

- 空状态提供明确下一步，不展示与MVP无关的复杂导航。

### 验收标准

- 用户点击任一入口均进入对应流程。

- 最近项目可打开并恢复上次状态。

- 示例数据不会覆盖用户真实数据。

## 9.2 简历导入与解析

### 支持输入

- 粘贴纯文本（P0）

- 文本型PDF（P0）

- DOCX（P1）

- 图片或扫描PDF OCR（P2）

- JSON Resume（P1）

### 处理流程

1. 文件类型、大小和加密状态校验。

2. 提取文本与基础段落/页码信息。

3. 调用Profile Builder Skill输出统一JSON。

4. 运行字段规则校验：日期、邮箱、手机号、重复经历、空标题。

5. 显示解析结果与原文对照，要求用户确认。

6. 用户确认后写入职业母档案；原文件与结构化数据分开保存。

### 解析输出最小字段

```text

{
  "basics": {"name":"", "phone":"", "email":"", "location":"", "summary":""},
  "education": [],
  "internships": [],
  "projects": [],
  "competitions": [],
  "campusActivities": [],
  "skills": [],
  "certificates": [],
  "works": [],
  "unclassifiedBlocks": []
}

```

### 异常处理

- PDF无可提取文本：提示“可能为扫描件”，允许转为粘贴文本或后续OCR。

- 模型输出不符合Schema：自动重试一次；仍失败则保留原文并进入手动分类。

- 字段置信度低：标黄并要求用户确认，不静默猜测。

- 文件包含敏感证件号码：默认不提取到简历字段，并给出删除提示。

## 9.3 职业母档案编辑器

### 核心结构

- 基本信息：姓名、联系方式、求职意向、地点、链接。

- 教育经历：学校、学院、专业、学历、时间、GPA/排名、课程。

- 实习/工作：组织、岗位、时间、任务、行动、工具、结果、证据。

- 项目经历：项目背景、角色、任务、方法、成果、技术栈、链接。

- 竞赛/校园/志愿：级别、角色、贡献、结果。

- 技能：技能名称、熟练度、使用证据、最近使用时间。

- 证书与作品：名称、颁发方、日期、文件或链接。

### 交互要求

- 模块支持新增、编辑、删除、复制、拖拽排序。

- 经历条目必须区分“事实原文”和“用于简历的表达稿”。

- 每条经历可绑定多个证据；证据可仅本地保存。

- 显示完整度提示，但不得通过虚假评分制造焦虑。

- 提供“让AI追问”按钮，围绕任务、行动、工具、结果和个人贡献提问。

- 所有AI补充内容先进入草稿，不直接覆盖已确认事实。

## 9.4 AI对话式经历挖掘

### 目标

帮助没有现成简历或描述过于简略的用户，将普通叙述转化为结构化、可验证的经历记录。

### 对话策略

- 一次只追问一个关键缺口，避免连续十几个问题。

- 优先追问用户个人贡献，不把团队成果全部归到用户身上。

- 对数字、排名、提升比例等结果要求来源或用户确认。

- 允许用户回答“不记得/没有数据”，随后使用定性描述。

- 完成一轮后输出事实摘要，请用户确认再存档。

### 示例

```text

用户：我参加了一个统计建模比赛。
AI：你在团队中具体负责哪一部分？
用户：我负责清洗数据和做回归。
AI：你使用了哪些工具？数据大约包含多少地区或样本？
用户：用了Stata，31个省。
AI：最终形成了什么结果或交付物？
用户：完成论文和汇报PPT。
→ 生成事实摘要并请求确认，不擅自添加获奖结果。

```

## 9.5 岗位JD管理与解析

### 输入方式

- 粘贴JD文本（P0）

- 手动填写岗位名称与公司（P0）

- 网页链接抓取（P2，需遵守站点规则）

- 截图OCR（P2）

### 结构化输出

| 字段 | 说明 |

| --- | --- |

| 岗位基本信息 | 岗位名、公司、行业、地点、工作类型 |

| 职责 Responsibilities | 入职后主要任务和业务对象 |

| 硬性条件 Must-have | 学历、专业、经验、语言、证书等不可轻易替代条件 |

| 核心技能 Core Skills | 工具、技术、方法、行业知识 |

| 通用能力 Soft Skills | 沟通、协作、执行、分析等 |

| 加分项 Nice-to-have | 非必需但能提升竞争力的条件 |

| 关键词 Keywords | 用于后续表达和检查，但不直接等于匹配度 |

| 权重/优先级 | 高、中、低，并解释判断依据 |

### 展示要求

- 每项要求显示原JD证据片段。

- 用户可编辑分类、权重和描述。

- 同义或重复要求应合并，但保留原文映射。

- 模糊要求如“抗压能力强”不能被当作高精度事实评分。

## 9.6 经历匹配与差距诊断

### 匹配状态

| 状态 | 定义 | 产品表现 |

| --- | --- | --- |

| 已直接满足 | 档案中存在明确且强相关证据 | 绿色；列出对应经历与证据 |

| 可间接证明 | 存在相关任务或迁移能力，但简历尚未表达 | 蓝色；建议如何说明 |

| 需要用户确认 | AI推断可能相关，但事实不足 | 黄色；发起追问 |

| 当前缺失 | 没有可证明经历或不满足硬性条件 | 灰/红；如实提示，不伪造 |

| 存在风险 | 经历描述可能夸大、冲突或缺少证据 | 红色；阻止静默写入 |

### 匹配逻辑

采用“规则校验 + 向量语义召回 + 大模型重排序/解释”的组合，而不是仅使用关键词命中。规则负责学历、日期、语言证书等确定性条件；向量召回寻找语义相关经历；大模型根据岗位上下文判断相关性、证据强度和可表达方式。

### 禁止行为

- 不得把技能名称出现一次就判断为“熟练掌握”。

- 不得用无法解释的单一总分替代具体诊断。

- 不得将“用户可以学习”表述为“用户已经具备”。

## 9.7 岗位分支与版本管理

### 核心规则

- 一个职业母档案可拥有多个岗位分支。

- 岗位分支保存选中的经历、排序、岗位专属表达、模板和导出记录。

- 母档案事实更新时，系统提示受影响分支；用户决定是否同步。

- 岗位专属改写默认不反向覆盖母档案事实。

- 分支可复制、重命名、归档和删除；删除前二次确认。

### 版本策略

- 每次接受AI建议或手动保存形成Revision。

- 支持撤销/重做与“与上一版本对比”。

- 记录修改来源：用户手动、AI建议、模板自动压缩。

## 9.8 AI建议卡片与可解释修改

### 卡片字段

```text

{
  "suggestionId": "...",
  "target": "project-123.description[0]",
  "type": "rewrite | add | remove | reorder | risk",
  "originalText": "...",
  "suggestedText": "...",
  "reason": "...",
  "jobRequirementIds": ["jr-1"],
  "evidenceIds": ["ev-2"],
  "newFacts": ["31个省级样本"],
  "riskLevel": "low | medium | high",
  "confidence": 0.82
}

```

### 交互动作

- 接受全部

- 部分接受/在建议上编辑

- 拒绝

- 重新生成

- 降低表达强度

- 恢复原文

- 查看依据

### 生成约束

- 建议必须引用至少一个岗位要求；内容型建议应引用至少一个用户经历或证据。

- 若出现母档案不存在的新数字、组织、技能或成果，必须标记newFacts。

- 高风险建议默认不可一键接受，必须用户确认事实。

- 对硬性条件缺失只能提示差距，不能通过措辞“补齐”。

## 9.9 事实校验与证据库

### 证据类型

- 文件：证书、报告、作品、实习证明

- 链接：GitHub、作品页、竞赛公告

- 文本说明：用户确认的事实备注

- 系统来源：原始导入简历片段

### 校验规则

- 对AI输出与母档案做实体差异：数字、日期、组织、工具、奖项、职责。

- 新增事实必须来自用户确认或证据。

- 冲突事实（如时间重叠、不同版本数字不一致）进入风险中心。

- 证据仅用于生成与核验，不默认展示在最终简历中。

## 9.10 自然表达与个人语气校准

该模块的目标是减少模板化和过度包装，使简历符合用户真实表达能力，而非规避AI检测。

| 模式 | 行为 |

| --- | --- |

| 保守真实 | 不主动强化结果；避免强因果和夸张动词 |

| 专业简洁 | 使用清晰动作动词，控制句长，删除重复背景 |

| 数据强化 | 优先使用用户已确认的规模、频次、效率和结果数据 |

| 正式商务 | 适用于国企、行政、传统行业，语气稳健 |

| 自然表达 | 减少同质句式，保留用户常用词和语言节奏 |

## 9.11 模板引擎与实时预览

### 设计原则

- 模板只读取结构化ResumeBranch数据，不存储事实。

- 模板定义包括字体、字号、间距、栏目布局、排序和分页规则。

- 所有模板必须保证机器可读文本，不以整页图片生成PDF。

- 首版至少两套：正式商务型、互联网简洁型。

### 分页与压缩

- 显示一页/两页状态和溢出模块。

- 提供自动压缩建议：减少空白、缩短低优先级内容、调整模块间距。

- 自动压缩不得改变事实，所有删减需可查看。

- 避免通过极小字体强行一页化；正文建议不低于9pt。

## 9.12 PDF导出

- 导出前检查：联系方式、日期格式、错别字、分页、空模块、未确认高风险事实。

- 支持自定义文件名，推荐“姓名_岗位_公司_日期.pdf”。

- 生成文本可选择、可复制的PDF。

- 导出记录关联到岗位分支，保存模板与Revision版本。

- 若存在高风险未确认事实，默认阻止导出或强提示。

## 9.13 求职材料联动（P1）

| 材料 | 输入 | 输出要求 |

| --- | --- | --- |

| 自我介绍 | 岗位JD + 当前简历分支 | 30秒/60秒/90秒版本；与简历事实一致 |

| 打招呼文案 | 岗位、公司、核心匹配点 | 简短、具体、不群发感；可人工修改 |

| 求职邮件 | 岗位、附件名称、联系信息 | 主题与正文；不虚构内推关系 |

| 面试问题预测 | 岗位要求 + 简历重点 | 问题、考察点、回答提纲和证据 |

| STAR回答 | 指定经历 + 问题 | 情境、任务、行动、结果；缺失项继续追问 |

## 9.14 投递看板（P1/P2）

投递看板用于管理用户主动投递的岗位，不在MVP中实现绕过平台的自动投递。

- 状态：收藏、待投递、已投递、已沟通、笔试、面试、Offer、拒绝、关闭。

- 每个岗位关联使用的简历分支和导出版本。

- 记录投递时间、渠道、联系人、下一步日期和备注。

- AI可建议优先级和提醒，但最终操作由用户确认。

- 后续浏览器扩展仅做字段辅助与本地复制，不处理验证码或反自动化绕过。

# 10. AI能力与Agent工作流设计

## 10.1 AI核心贡献

若移除AI，产品将退化为结构化简历编辑器和模板工具；无法完成非结构化经历理解、岗位语义拆解、经历证据匹配、针对性重组、事实差异检测和自然表达校准。因此AI属于核心能力而非附加功能。

## 10.2 Agent/Skill清单

| Agent/Skill | 职责 |

| --- | --- |

| Profile Builder | 将PDF/文本解析为职业母档案JSON；识别未分类内容 |

| Experience Miner | 通过追问补充任务、行动、工具、结果和个人贡献 |

| JD Analyzer | 拆解职责、硬性条件、技能、加分项与优先级 |

| Evidence Matcher | 召回并重排序与岗位要求相关的真实经历和证据 |

| Resume Tailor | 选择、排序、压缩和重写岗位分支内容 |

| Fact Guard | 检测新增事实、冲突、夸大和证据缺失 |

| Tone Calibrator | 根据用户选择调整正式度、简洁度和自然度 |

| Material Generator | 生成自我介绍、沟通文案和面试问题 |

| Workflow Orchestrator | 维护任务状态、调用Skills、合并结构化结果和重试 |

## 10.3 编排流程

```text

[简历输入]
  → Profile Builder
  → Schema Validator
  → 用户确认
  → CareerProfile

[岗位JD]
  → JD Analyzer
  → JobRequirements
  → Evidence Matcher（规则 + 向量召回 + LLM重排）
  → MatchMatrix
  → Resume Tailor
  → Fact Guard
  → AI建议卡片
  → 用户确认
  → ResumeBranch
  → Template Renderer / Material Generator

```

## 10.4 模型调用策略

- 所有关键输出使用JSON Schema或强类型结构化输出。

- 低风险任务（分类、摘要）可使用成本较低模型；复杂匹配与改写使用能力更强模型。

- 同一请求最多自动重试1—2次，避免无限消耗。

- 长简历先分块抽取，再合并；不要一次把所有文件无差别塞入上下文。

- 提示词中明确“仅根据提供事实生成”；系统层与用户层分离。

- 模型接口抽象化，便于替换通义千问、DeepSeek、Claude、OpenAI或本地模型。

## 10.5 事实保护机制

1. 原始导入文本、用户确认事实、AI表达稿分层存储。

2. AI改写后运行实体差异检测。

3. 新增数字/实体被标记并绑定确认状态。

4. Fact Guard根据证据和母档案判定风险。

5. 高风险建议无法直接批量接受。

6. 导出前再次执行最终一致性检查。

## 10.6 Prompt与Skill管理

- 每个Skill拥有独立版本号、输入Schema、输出Schema、示例和失败降级策略。

- Prompt不可散落在前端组件中，应集中放置在AI模块。

- 记录模型、Prompt版本、输入摘要、输出和用户采纳结果，用于后续评估。

- 禁止把完整手机号、邮箱等不必要敏感信息发送给模型；解析前可脱敏。

# 11. 数据模型

## 11.1 核心实体关系

```text

User/Workspace
  └── CareerProfile
       ├── BasicInfo
       ├── Experience[]
       ├── Skill[]
       ├── Certificate[]
       ├── Evidence[]
       └── ResumeBranch[]
             ├── JobDescription
             │    └── JobRequirement[]
             ├── RequirementMatch[]
             ├── SelectedContent[]
             ├── AiSuggestion[]
             ├── ResumeRevision[]
             ├── TemplateConfig
             └── ExportRecord[]

```

## 11.2 主要实体字段

| 实体 | 关键字段 |

| --- | --- |

| CareerProfile | id, name, basics, preference, createdAt, updatedAt, version |

| Experience | id, type, organization, role, startDate, endDate, facts, resumeDrafts, tags, evidenceIds |

| Evidence | id, type, title, filePath/url, extractedText, privacyLevel, verifiedAt |

| JobDescription | id, title, company, rawText, source, parsedAt |

| JobRequirement | id, category, description, priority, hardConstraint, sourceSpan |

| RequirementMatch | requirementId, experienceIds, evidenceIds, status, score, explanation |

| ResumeBranch | id, profileId, jobId, name, selectedItems, customTexts, templateId, currentRevisionId |

| AiSuggestion | id, targetPath, type, original, suggested, reason, newFacts, risk, status |

| ResumeRevision | id, branchId, snapshot, source, createdAt |

| ExportRecord | id, branchId, revisionId, templateId, format, fileName, createdAt |

## 11.3 数据继承规则

- 母档案保存事实层；岗位分支保存选择层和表达层。

- 岗位分支引用Experience ID，不复制全部事实；岗位专属文本覆盖在分支中。

- 删除母档案经历前检查引用分支并提示影响。

- AI建议作用于分支草稿；只有用户明确操作才能更新母档案事实。

# 12. 推荐系统架构与技术边界

## 12.1 原型架构

```text

Web/Desktop Frontend
  ├── 项目空间与编辑器
  ├── 简历实时预览
  └── AI建议交互
        │
Application Service
  ├── Career Profile Service
  ├── Job/Branch Service
  ├── Revision Service
  └── Export Service
        │
AI Orchestration Layer
  ├── Skill Registry
  ├── Model Adapter
  ├── Schema Validator
  ├── Fact Guard
  └── Embedding/Vector Search
        │
Storage
  ├── SQLite/PostgreSQL
  ├── Local/Object File Storage
  └── Vector Index

```

## 12.2 推荐技术栈（可根据现有能力调整）

| 层 | 建议 |

| --- | --- |

| 前端 | Next.js/React + TypeScript + Tailwind + shadcn/ui；或沿用熟悉的Vite React |

| 桌面/本地优先 | Tauri 2（可选），适合隐私和本地文件；比赛在线演示可先Web |

| 后端 | Next.js Route/API或FastAPI；MVP避免过早拆微服务 |

| 数据库 | SQLite（本地原型）或PostgreSQL（在线版） |

| 文档解析 | PDF.js/pdf-parse；Mammoth用于DOCX；OCR后置 |

| 模板/PDF | React HTML模板 + Playwright/Puppeteer打印PDF |

| AI | 统一ModelAdapter接入实际模型；结构化输出 + Zod/Pydantic校验 |

| 向量检索 | 首版可用本地向量库或数据库pgvector；数据量小也可先内存检索 |

## 12.3 API边界示例

```text

POST /api/profiles/import
GET  /api/profiles/:id
PATCH /api/profiles/:id
POST /api/profiles/:id/mine-experience

POST /api/jobs/analyze
POST /api/jobs/:id/match/:profileId

POST /api/branches
GET  /api/branches/:id
POST /api/branches/:id/suggestions
POST /api/branches/:id/apply-suggestion
POST /api/branches/:id/export

POST /api/ai/validate-facts
POST /api/materials/generate

```

> 若采用本地Tauri应用，可将这些边界实现为前端服务层与Rust/本地Node命令，不必强制部署独立服务器。

# 13. 非功能需求

## 13.1 性能

- 常规文本简历导入的首屏反馈≤2秒，完整AI解析目标≤30秒。

- 岗位JD解析目标≤15秒；生成首批建议目标≤30秒。

- 简历编辑和模板切换实时响应，关键交互延迟≤200ms。

- 长任务必须显示阶段进度和可取消操作。

## 13.2 稳定性

- AI请求失败不应导致用户已编辑内容丢失。

- 自动保存间隔≤10秒；关闭页面前有未保存提示。

- Schema验证失败可重试或进入手动模式。

- 导出失败应保留当前版本并给出可操作原因。

## 13.3 可访问性与兼容性

- 核心操作可通过键盘完成；表单具备可见标签。

- 颜色状态同时使用文字或图标，不仅依赖红绿。

- 优先支持Chrome/Edge最新两个主版本；导出PDF在常见阅读器可正常复制文本。

## 13.4 可维护性

- AI Prompt、Schema、业务规则与UI解耦。

- 模板以配置和组件方式扩展，不复制整套业务逻辑。

- 关键流程有日志和可复现的输入输出快照（脱敏）。

# 14. 隐私、安全与合规

## 14.1 隐私原则

- 简历属于高敏个人信息，默认最小化收集和使用。

- 明确说明哪些内容会发送给第三方模型；优先脱敏姓名、手机号、邮箱和地址。

- 用户可下载职业母档案JSON、全部简历分支和原始文件。

- 用户可一键删除数据；删除后不再用于模型评估。

- 证据文件默认私有，不公开生成可猜测链接。

## 14.2 投递合规

- 不提供验证码破解、Cookie窃取、反风控绕过或无人值守海量投递。

- 所有外部提交必须由用户明确确认。

- 如后续接入智联招聘等平台，应使用平台授权接口或合作机制。

- 职位数据抓取需遵守robots、服务条款、频率限制和版权要求。

## 14.3 AI合规与透明性

- 在产品和比赛材料中标注使用的模型、开源组件和第三方API。

- AI生成内容显示“AI建议”来源，并保留用户修改痕迹。

- 不宣称模型能够客观预测录用概率或消除招聘偏见。

# 15. 异常、失败与边界场景

| 场景 | 可能原因 | 处理策略 |

| --- | --- | --- |

| PDF无法解析 | 加密、扫描件、字体编码异常 | 提示原因；允许粘贴文本；保留文件；后续OCR |

| AI输出格式错误 | 模型未遵循JSON Schema | 自动修复/重试一次；失败转手动模式 |

| JD过短 | 仅有岗位名称或几句话 | 要求补充；允许生成低置信度分析并显式标记 |

| 岗位要求矛盾 | 年限、学历等文本冲突 | 保留冲突原文并要求用户判断 |

| 经历无结果数据 | 用户没有量化结果 | 使用定性事实，不制造数字 |

| 一页溢出 | 内容过多 | 给出删减优先级和间距建议；不静默删内容 |

| 模型超时/额度不足 | 第三方API异常 | 保留状态；支持重试或切换模型；不丢数据 |

| 用户拒绝全部建议 | AI价值不足或风格不合 | 允许手动编辑；收集拒绝原因用于评估 |

| 分支与母档案冲突 | 母档案事实更新 | 显示差异，用户选择同步策略 |

# 16. 数据埋点与验证方案

## 16.1 关键事件

- profile_create_started / completed

- resume_import_started / parsed / confirmed / failed

- jd_analyze_started / completed / edited

- match_viewed

- suggestion_generated / accepted / edited / rejected

- branch_created / exported

- template_switched

- high_risk_fact_confirmed / removed

## 16.2 小样本验证设计

1. 选择3—5名不同专业大学生，收集其真实简历和2类岗位JD。

2. 人工标注简历模块、岗位要求及相关经历，作为对照。

3. 记录导入耗时、字段正确率、建议采纳率和导出耗时。

4. 让用户对“相关性、真实性、自然度、可解释性、易用性”进行5分评价。

5. 对同一母档案生成两个岗位分支，人工检查差异是否合理。

## 16.3 比赛阶段建议展示指标

- 从导入到首份岗位专属简历的耗时对比。

- 同一用户面向两个岗位的经历选择差异。

- AI建议中具备明确岗位依据和事实依据的比例。

- 用户接受/编辑/拒绝建议的分布。

- 发现并阻止的新增事实示例。

# 17. MVP验收标准

## 17.1 功能验收

| 模块 | 验收标准 |

| --- | --- |

| 导入 | 可上传至少1份文本型PDF或粘贴文本；生成结构化档案；用户可校对 |

| 档案 | 可新增/编辑/删除教育、实习、项目、技能；数据持久化 |

| JD | 可粘贴JD并输出职责、硬性条件、技能和优先级 |

| 匹配 | 每条核心要求显示对应经历或“当前无证据” |

| 建议 | 至少生成3类建议；每条显示原因、岗位依据、事实风险 |

| 确认 | 接受/拒绝/编辑后预览即时更新；可撤销 |

| 分支 | 同一母档案可保存至少2个不同岗位分支 |

| 模板 | 至少2套模板可切换；内容不丢失 |

| 导出 | 可导出文本可复制的PDF；文件与预览基本一致 |

| 安全 | 高风险新增事实未经确认不能静默进入最终导出 |

## 17.2 Demo验收

- 使用同一名学生的母档案，分别导入“数据分析实习”和“外贸运营实习”JD。

- 两个岗位分支至少在经历选择、排序、技能或项目表达中的两项存在明显差异。

- 演示一条AI新增数字被Fact Guard标记并要求用户确认。

- 演示用户拒绝建议、接受建议、撤销和切换模板。

- 成功导出两份PDF。

# 18. 开发计划与Sprint拆分

## 18.1 开发优先级原则

> **先完成“导入—档案—JD—匹配—建议—分支—模板—PDF”的纵向闭环；任何不能直接增强该闭环的功能都后置。**

## 18.2 建议Sprint

| Sprint | 范围 | 完成定义 |

| --- | --- | --- |

| Sprint 0｜半天 | 工程初始化、数据Schema、示例数据、页面骨架 | 能打开项目；数据模型可序列化；示例档案可显示 |

| Sprint 1｜1天 | 职业母档案编辑器、粘贴文本导入、持久化 | 可创建/编辑/保存完整档案 |

| Sprint 2｜1天 | PDF文本提取、Profile Builder、解析确认页 | 导入常见PDF并生成可校对JSON |

| Sprint 3｜1天 | JD Analyzer、要求列表、证据片段 | JD可结构化并人工编辑 |

| Sprint 4｜1天 | Evidence Matcher、匹配矩阵、差距诊断 | 要求能关联经历并解释 |

| Sprint 5｜1天 | Resume Tailor、建议卡片、Fact Guard | 建议可接受/拒绝/编辑；新增事实被标记 |

| Sprint 6｜1天 | 岗位分支、版本历史、撤销 | 至少两个分支独立保存 |

| Sprint 7｜1天 | 两套模板、实时预览、PDF导出 | 成功导出可复制文本PDF |

| Sprint 8｜1天 | 求职材料、演示数据、稳定性和视觉收口 | 完成比赛演示脚本和录屏 |

## 18.3 单人开发执行规则

- 每个Sprint只解决一个可验收目标，完成后更新plan.md和history.md。

- 禁止在核心链路未完成时大规模重构视觉或增加登录、支付。

- AI能力先使用真实API跑通，再考虑多模型、缓存和成本优化。

- 每天保存一段可演示录屏，避免最后一天才发现流程不可用。

# 19. 比赛Demo展示脚本

## 19.1 建议时长：3—4分钟

| 时间 | 画面 | 讲解重点 |

| --- | --- | --- |

| 0:00—0:20 | 痛点与定位 | “大学生缺的不是模板，而是把真实经历适配到不同岗位的能力。” |

| 0:20—0:50 | 导入简历 | 上传一份已有PDF，展示结构化成职业母档案并可编辑。 |

| 0:50—1:20 | 岗位A分析 | 粘贴数据分析岗位JD，展示硬性条件、技能和优先级。 |

| 1:20—2:00 | 匹配与建议 | 展示要求—经历映射、可解释建议和事实风险标记。 |

| 2:00—2:30 | 生成岗位分支 | 接受部分建议，切换模板，导出PDF。 |

| 2:30—3:10 | 岗位B对比 | 切换外贸运营岗位，展示经历选择和表达变化。 |

| 3:10—3:40 | 延伸与价值 | 生成自我介绍/面试问题，说明人机协同投递和商业路径。 |

## 19.2 必须准备的截图与证据

- 原始PDF简历与解析后的母档案对照。

- 同一份母档案的两个岗位JD分析结果。

- 岗位要求—经历证据映射截图。

- AI建议卡片：原文、建议、原因和真实性提醒。

- Fact Guard阻止虚构数字的案例。

- 两个岗位分支简历对比。

- 两套模板切换和最终PDF。

- 小样本测试结果或用户反馈。

# 20. 商业模式与落地路径

## 20.1 C端

- 免费：职业母档案、基础模板、有限次数岗位分析。

- 订阅/点数：深度岗位适配、更多分支、英文简历、面试材料、高级模板。

- 毕业季短期包：按月或按求职季收费，降低长期订阅阻力。

## 20.2 B端/B2B2C

- 高校就业中心：学生职业档案建设、简历辅导、课程与工作坊。

- 招聘平台：在用户授权下提供标准化档案、岗位适配与材料生成能力。

- 企业校招：帮助候选人按岗位结构化提交材料，减少无关信息。

## 20.3 试点设想

首期可在高校内招募20—50名学生试用，覆盖经济、计算机、管理等专业。以“完成首份岗位分支的时间、建议采纳率、岗位理解提升、材料满意度”为验证指标。成熟后再与高校就业指导部门或智联招聘探索接口与场景合作。

# 21. 关键风险与应对

| 风险 | 等级 | 应对 |

| --- | --- | --- |

| AI幻觉与夸大 | 高 | 事实分层、实体差异检测、强制确认、证据库 |

| PDF解析不稳定 | 高 | MVP聚焦文本型PDF；保留粘贴文本降级；OCR后置 |

| 范围过大 | 高 | P0闭环优先；投递、高校端、复杂导出后置 |

| 模板与分页复杂 | 中高 | 首版仅两套固定模板；统一渲染引擎和分页规则 |

| 用户不信任AI | 中高 | 展示依据、风险、原文对比和可撤销操作 |

| API成本/限额 | 中 | 模型路由、缓存、限制重试、可替换模型接口 |

| 隐私风险 | 高 | 本地优先、脱敏、明确第三方传输、删除与导出能力 |

| 比赛演示不稳定 | 高 | 预置示例数据、缓存结果、离线降级、提前录制备用视频 |

| 与现有产品同质化 | 中高 | 集中展示职业母档案、多岗位分支、证据匹配和事实保护 |

# 22. 待决策问题

- 首版采用纯Web还是Tauri本地应用？比赛展示便利性与隐私需权衡。

- 实际使用哪个主模型、嵌入模型和备用模型？需要在技术说明中明确。

- DOCX导入是否进入P0？若时间不足，可先仅支持PDF文本和粘贴。

- 是否在V1.0开放用户注册，还是使用本地工作区/匿名体验？

- 岗位匹配是否展示总分？建议MVP只展示分项状态和说明，避免虚假精确。

- 项目最终中文品牌、Logo和域名是否固定为“职适AI”？

# 23. 开发启动清单

1. 确认应用形态、技术栈和实际AI模型。

2. 冻结CareerProfile、JobRequirement、ResumeBranch和AiSuggestion的Schema。

3. 准备一份真实但可脱敏的学生母档案和两个岗位JD作为贯穿测试数据。

4. 搭建页面骨架和本地持久化。

5. 先实现粘贴文本→结构化档案，不等待PDF解析完美。

6. 逐步接入JD解析、匹配和建议，并为每一步保存中间结果。

7. 完成第一套模板和PDF导出后再增加第二套模板。

8. 建立Fact Guard和高风险确认门槛。

9. 按MVP验收表逐项测试并记录截图。

10. 完成演示视频、PPT、说明书和模型/开源来源清单。

> **开发过程中始终用一句话检查范围：这个功能是否直接增强“职业母档案 → 岗位适配 → 可解释生成”的核心闭环？若不是，后置。**

# 附录A：示例岗位适配结果

## A.1 用户母档案片段

- 统计建模：使用Stata清洗31个省级样本，完成描述统计、相关分析与区域差异分析。

- AI应用项目：参与需求梳理、原型设计和功能验收。

- 政府部门实习：处理公文流转和市长热线记录。

- 英语与外贸学习：CET-4 601，了解外贸业务流程。

## A.2 数据分析岗位

- 优先保留统计建模经历并前置。

- 突出Stata、Excel、数据清洗、样本规模和分析交付物。

- AI应用项目缩短为辅助经历，强调需求数据和验证。

- 政府实习仅保留记录准确性和信息处理。

## A.3 外贸运营岗位

- 前置英语、沟通、流程理解和信息整理能力。

- 将AI应用项目表达为跨工具协同、需求分析和执行。

- 统计建模经历保留数据分析和市场研究迁移能力，但降低技术细节。

- 不得把“了解外贸流程”改写为“具备独立开发客户经验”。

# 附录B：建议目录结构

```text

src/
├── app/ or pages/
├── components/
│   ├── profile/
│   ├── job/
│   ├── resume/
│   ├── suggestions/
│   └── templates/
├── domain/
│   ├── profile/
│   ├── job/
│   ├── branch/
│   └── evidence/
├── ai/
│   ├── adapters/
│   ├── skills/
│   │   ├── profileBuilder.ts
│   │   ├── experienceMiner.ts
│   │   ├── jdAnalyzer.ts
│   │   ├── evidenceMatcher.ts
│   │   ├── resumeTailor.ts
│   │   └── factGuard.ts
│   ├── prompts/
│   ├── schemas/
│   └── orchestrator.ts
├── services/
│   ├── storage.ts
│   ├── documentParser.ts
│   ├── exportPdf.ts
│   └── revision.ts
└── tests/
    ├── fixtures/
    ├── unit/
    └── e2e/

```